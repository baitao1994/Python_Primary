********************************************

Text() 与 Entry() 的异同：

Text() 可以直接编写文本，以及装进画布，如：Text(Point(2,4),"我的妈呀").draw(win)
但是Entry()只能先定义，再写入文字，装入画布，如：
input = Entry(Point(2,5),5)
input.setText("我的妈呀")
input.draw(win)

*******************************************
